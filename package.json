{
  "name": "@openclaw/clawaegisex",
  "version": "2026.6.22",
  "private": true,
  "description": "OpenClaw safety guard plugin for prompt, tool, and tool-result hardening.",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
